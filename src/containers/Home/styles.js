import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  root: {
    maxWidth: 345,
    margin: '8px',
  },
  media: {
    height: 140,
  },
  postContainer: {
    display: 'flex',
    flexWrap: 'wrap',
  },
  progress: {
    color: '#0D475E',
  },
});

export default useStyles;
